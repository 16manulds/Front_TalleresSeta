@* @model Front_TalleresSeta.Modelos.ModelosView.ViewInventarioMarca

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/Interfaz/_LayoutVerticalInterfaz.cshtml";
}

<div class="card shadow mb-lg-4">
    <div class="card-header py-3">
        <div class="row">
            <div class="col-md-4">
                <div class="form-group">
                    <span class="nav-link">
                        <i class="fab fa-wpforms"></i>
                        Eliminar Marca
                    </span>
                </div>
            </div>                        
        </div>
    </div>

    <div class="card-body">
        <div class="row mb-3">
            <label class="col-sm-3 col-form-label">HABILITADO</label>
            <div class="col-sm-2">
                @if (Model.Habilitado)
                {
                    <i class="fas fa-check-square text-success"></i>
                }
                else
                {
                    <i class="far fa-window-close text-danger"></i>
                }
            </div>
        </div>
        <hr />

        <div class="row mb-3">
            <label class="col-sm-3 col-form-label">MARCA</label>
            <div class="col-sm-6">
                <span class="label-info">@Model.NombreMarca</span>
            </div>
        </div>

        <div class="row mb-3">
            <label class="col-sm-3 col-form-label">FECHA REGISTRO</label>
            <div class="col-sm-6">
                <span class="label">@Model.FechaRegistro.ToShortDateString()</span>
            </div>
        </div>

        <div class="row mb-3">
            <label class="col-sm-3 col-form-label">DETALLE</label>
            <div class="col-sm-3">
                <span class="label-default">@Model.Detalle</span>
            </div>
        </div>

        <div class="row mb-3">
            <label class="col-sm-3 col-form-label">TALLER</label>
            <div class="col-sm-3">
                <span class="label-default">@Model.RazonSocialTaller</span>
            </div>
        </div>

        <hr />
        
    </div>
</div> *@